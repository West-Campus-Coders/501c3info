What you will need to have installed.
Git
>Guide https://git-scm.com/book/en/v2/Getting-Started-Installing-Git
>Download link https://git-scm.com/downloads

Backend:
1. Python
>https://www.python.org/downloads/
>If using windows to make things easier on you make sure you add to path while installing.
2. Python module Pandas
>This is a python module used for data science and document manipulation we will use it to edit and draw data from a large csv file.
>From terminal/cmd/git bash run  pip install pandas
3. Python Module BeautifulSoup
>This is a python module used for parsing XML documents.
>from terminal/cmd/git bash run pip install beautifulsoup4


Frontend:
1. Node.js / NPM
>Node. js is an open-source JavaScript scalable development environment that allows users to build full-scale web applications. Npm is the management system for Node. js packages and is the primary support for the JavaScript community that keeps the system functional.
>https://nodejs.org/en/download/current
>When you clone the repository you will have all the already created files however normally you would use NPM to create all these files for use with REACT.
